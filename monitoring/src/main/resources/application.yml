spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/sys"
    username: "root"
    password: "1234"

  jpa:
    hibernate:
      ddl-auto: create

logging.level:
  org.hibernate.SQL: debug

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  tomcat:
    mbeanregistry:
      enabled: true
